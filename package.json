{
  "name": "scb",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:api": "wrangler deploy --config wrangler-api.jsonc",
    "deploy:frontend": "wrangler deploy --config wrangler-frontend.jsonc",
    "deploy": "npm run build && npm run deploy:api && npm run deploy:frontend"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "leaflet": "^1.9.4"
  }
}
